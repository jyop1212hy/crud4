spring:
  datasource:
    url: jdbc:mysql://localhost:3306/crud_db4
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update   # ???: create / update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080